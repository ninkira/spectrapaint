# Frontend
spectrapaint/node_modules/
spectrapaint/dist/
*.log

# Backend Python
backend/.venv/
__pycache__/
*.pyc
.env
.env.*

# Local HSI data
backend/app/data/testdata/
*.hdr
*.img

# Packaged build output (PyInstaller + Inno Setup), built at the repo top level.
# The repo is in OneDrive; pause OneDrive sync while building to avoid file locks.
/dist/
/build/
/*Setup.exe

# Local SQLite database (created next to the app)
/app.db*

# OS junk
.DS_Store
Thumbs.db
