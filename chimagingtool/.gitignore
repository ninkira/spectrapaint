# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local
.idea/
*.venv/

# Python / PyInstaller build output
build/
__pycache__/
*.pyc
# NOTE: `dist` (above) already covers imaging-app/dist and backend/dist.
# The packaged app + installer are built OUTSIDE the repo (C:\ImagingToolBuild) to dodge
# OneDrive file-locking — see docs/packaging-guide.md.

# Local-only working docs (kept out of the repo for now)
docs/

# Imaging data — user-provided, never committed (large HSI cubes also covered by *.hdr/*.img)
backend/app/data/

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
